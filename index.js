"use strict";

import { Library } from "./library.js";

export default {
  Library,
};

// let libraries = Library.listAll();

// libraries.forEach((ele) => {
//   console.log(`LIB: ${ele.lib} VER: ${ele.version}`);
// });
